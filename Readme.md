# Description

This is a UDP echo server application that has two main components, namely a client side and a server side.

The client and server Java programs exchange messages over the User Datagram Protocol (UDP).
